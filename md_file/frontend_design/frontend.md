- [ ] url为index：显示众多游戏的图标以及在各自下方写上游戏名，在页面右侧需要直接显示一个智能AI助手，在页面上方需要具有一个搜索栏，预设信息："请输入您要搜索的游戏(本功能暂时未实现)"。在用户完成输入后点击搜索，跳转到对应页面(通过和后端的交互)，通过前端页面模板根据 URL 参数 `game_name` 动态加载不同游戏的数据。
- [ ] 智能AI助手交互详细信息：本质上是用户通过前端页面不断将数据发送到后端节点，后者不断进行处理
- [ ] 前端传输用户请求给后端时，每次都要传输用户id，而用户通过智能AI助手发送请求时，还需要将线程ID也一并发过去

